# Claude Code local settings (machine-specific, not for version control)
.claude/

# ARM template outputs from local Bicep builds
infra/bicep/*.json
infra/bicep/modules/*.json

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
*.swp
*.bak
.vscode/settings.json

# Secrets — never commit real parameter files with values filled in
*.local.parameters.json
.env

# Temp files from build scripts
*.log

# Jekyll / Bundler
vendor/
.bundle/
docs/_site/
Gemfile.lock
